<catel:Window x:Class="Orc.Extensibility.Example.Views.MainWindow"
              xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
              xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
              xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
              xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
              xmlns:catel="http://schemas.catelproject.com"
              xmlns:orccontrols="http://schemas.wildgums.com/orc/controls"
              xmlns:orctheming="http://schemas.wildgums.com/orc/theming"
              xmlns:orclogviewer="http://schemas.wildgums.com/orc/logviewer"
              mc:Ignorable="d"
              d:DesignHeight="300"
              d:DesignWidth="400">

    <orccontrols:StackGrid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <orctheming:ThemeSwitcherView />
            <orctheming:FontSizeSwitcherView />
        </StackPanel>

        <Label>
            <TextBlock TextWrapping="Wrap">
                <catel:LanguageBinding ResourceName="MainWindow_Description" />
            </TextBlock>
        </Label>

        <orccontrols:StackGrid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Label Content="{catel:LanguageBinding MainWindow_SelectedPlugin}" />
            <ComboBox ItemsSource="{Binding AvailablePlugins}"
                      SelectedItem="{Binding SelectedPlugin, Mode=TwoWay}" />
        </orccontrols:StackGrid>

        <Label>
            <TextBlock TextWrapping="Wrap"
                       FontStyle="Italic">
                <catel:LanguageBinding ResourceName="MainWindow_PluginChangeNote" />
            </TextBlock>
        </Label>

        <Rectangle Height="50"
                   HorizontalAlignment="Stretch">
            <Rectangle.Fill>
                <SolidColorBrush Color="{Binding Color}" />
            </Rectangle.Fill>
        </Rectangle>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <GroupBox Grid.Row="0"
                      Grid.Column="0"
                      Header="{catel:LanguageBinding MainWindow_RuntimeExtractedAssemblies}">
                <ListBox ItemsSource="{Binding RuntimeAssemblies}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding Source}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ListBox>
            </GroupBox>

            <GroupBox Grid.Row="1"
                      Grid.Column="0"
                      Header="{catel:LanguageBinding MainWindow_RuntimeResolvedAssemblies}">
                <ListBox ItemsSource="{Binding RuntimeResolvedAssemblies}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Label Content="{Binding Source}" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ListBox>
            </GroupBox>

            <GroupBox Grid.RowSpan="2"
                      Grid.Column="1"
                      Header="{catel:LanguageBinding MainWindow_Logging}">
                <orclogviewer:AdvancedLogViewerControl />
            </GroupBox>
        </Grid>
    </orccontrols:StackGrid>

</catel:Window>



